class DefaultConfig():
    secret_key = "CHANGEME"  # Django secret key used for sessions. Make this a long random string and keep it a secret
    tmdb_api_key = None  # API key for The Movie Database (themoviedb.org).
